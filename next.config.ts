import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["via.placeholder.com"], // Use the correct image host domain
  },
  /* config options here */
};

export default nextConfig;


